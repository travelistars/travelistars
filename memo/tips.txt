Google: travelistars@gmail.com/kt
git: travelistars@gmail.com/kt1
Heroku: travelistars@gmail.com/kt1

Env:
- travelistars-dev
- travelistars-qa
- travelistars
# to install Ruby & Rails 
- sudo apt-get update
- sudo apt-get install git
- sudo apt-get install curl g++
- sudo apt-get install zlib1g-dev libssl-dev libreadline-dev libyaml-dev libxml2-dev libxslt-dev
- sudo apt-get install sqlite3 libsqlite3-dev nodejs
- git clone git://github.com/sstephenson/rbenv.git .rbenv
- echo 'export PATH="$HOME/.rbenv/bin:$PATH"' >> ~/.bashrc
- echo 'eval "$(rbenv init -)"' >> ~/.bashrc
- exec $SHELL
- mkdir -p ~/.rbenv/plugins
- cd ~/.rbenv/plugins
- rbenv install 2.2.2
- rbenv global 2.2.2
- cd && vim .gemrc
  1 install: --no-ri --no-rdoc
  2 update: --no-ri --no-rdoc
- gem install rails --version="~> 4.2"
- git clone git://github.com/sstephenson/ruby-build.git
- rbenv rehash

Git:
- branch: dev/qa/master
# to config your info
- git config  --global user.name "******"
- git config  --global user.email "*******@gmail.com"
- git config --global core.editor vim
# to get all data from repository
- git clone https://github.com/travelistars/travelistars
- git config --list
# to switch branch
- git checkout origin dev


Gemfile
# to install libs
- bundle install
# to list all installed files
- gem list

Heroku cmds:
# to login
- heroku login
# to open the window
- heroku open --app travelistars-dev
# to tail the log
- heroku logs --tail --app travelistars-dev
# to run the bash
- heroku run bash --app travelistars-dev

Rails:
# to start the server
- forman start
- bundle exec binstubs/unicorn -C ./config/unicorn.rb
# to update DB
- rake db:migrate

PostgreSQL:
# to install PostgreSQL & rails adaptor
- sudo apt-get install postgresql postgresql-contrib libpq-dev
- sudo vim /etc/postgresql/9.4/main/pg_hba.conf
- sudo /etc/init.d/postgresql restart
# to create roles after installing PostgreSQL
- sudo -u postgres psql -f script/db_init.psql
# to login
- psql travelistars_development
# to check DB roles
- \du
# to check DBs
- \l
# connect to DB
- \connect travelistars_development
# to check tables
- \d

TroubleShooting:
# Gem::LoadError: You have already activated rake 10.5.0, but your Gemfile requires rake 10.4.2.
- bundle update rake

